# LiarLiar

Writing exposes one schema-backed `write(text)` option; timeout fallback stays a raw safe default lie.
Vote actions should stay short `vote(optionId)` labels because `agentView` already lists option text.
Do not reveal author/truth fields to players before results; keep hidden-info filtering in `project()`.
Generated fallback lies must remain deterministic and must not equal the real answer.
Run checks from the games repo root: `node scripts/test-game-logic.mjs ./liarliar --sweep`.
