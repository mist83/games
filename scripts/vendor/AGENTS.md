# scripts/vendor/

Vendored files here let UI tests run without network or the main monorepo.
Keep `pixi-v8.17.0.min.js` byte-for-byte aligned with the host platform file when the host version changes.
Do not hand-edit minified vendor files.
